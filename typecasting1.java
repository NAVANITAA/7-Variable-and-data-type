import java.util.*;

public class typecasting1 {
    public static void main(String arg[]) {
        Scanner sc = new Scanner(System.in);

        // int a = sc.nextfloat();
        // float is larger than int, so int can't store the whole data

        float a = sc.nextInt();

        System.out.println(a);
    }

}
